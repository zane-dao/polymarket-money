# 2026-07-22 16:09 Asia/Singapore｜前端信息层级与紧凑表单

## 目标

按少技术债、奥卡姆剃刀和易用原则，改进数据集、策略、回测页面的信息优先级、强调方式、下拉框与字段空间利用率。

## 事实与证据

- 原 `.form-grid` 使用固定等宽两列，短字段和长选择器缺少内容感知的宽度规则。
- 策略参数合同原先只有类型和上下限；策略版本已有 `createdAtUtc` 但页面未展示。
- 数据集 manifest 没有统一可信的生成时间字段，不能用扫描时间或文件时间补造。
- 验证证据见 `reports/batches/batch-07-desktop-consolidation/FRONTEND-INFORMATION-ARCHITECTURE.md`。

## 修改

- AI 项目层：更新当前状态、报告索引和本会话摘要。
- 代码层：新增统一响应式表单栅格和格式化函数；重排数据集、策略、回测三页；扩展后端策略展示元数据及严格前端解析。
- 外部状态：未启动公网采集，未修改或终止已有4174进程；临时4374验证服务已停止。

## 验证

- 前端 Vitest `23/23`、全仓 Node `254/254`、Playwright 桌面/移动 `4/4`。
- 前端与全仓 TypeScript、Vite production build、`git diff --check` 通过。
- 人工检查三页 1440×1000 截图。

## 决定

- 展示语义由后端策略注册表拥有，前端不维护平行策略字典。
- 缺少可信数据集生成时间时明确显示“未记录”。
- 技术身份保留但默认折叠，不能删除用于审计的 ID 和哈希。

## 未决问题

- 其余六个页面尚未逐页采用新的内容感知表单栅格；本轮只修改连续工作流的三个高优先级页面。
- 4174 的 4273 后端仍指向既有不可变 candidate；删除命令需要获批重建并重启 staging 后端。

## 同轮补充：数据语义、可用数据与删除

- staging-sim 原为空是因为数据根无托管发布且未登记外部发布；现已登记仓库外标准化历史源，4174
  实时接口可见 2 个哈希验证版本。
- 新发布采用 `normalized-dataset-manifest-v2`，展示元数据不参与内容哈希；旧 V1 manifest 和 DTO
  均兼容读取。
- 数据集 DTO V2 增加名称、说明、发布时间、来源、标签和管理归属。
- 数据集、策略版本和终态回测新增安全删除；精确确认、引用保护、状态保护与路径边界由后端执行。
- 验证：全仓 Node `254/254`、前端 Vitest `23/23`、TypeScript 与 Vite production build 通过。

## 下一步

- 使用真实长名称、多版本和多数据集样本继续验证选择器；需要时再把相同最小规则推广到其余页面。

## 回测失败跟进

- 4174 中 3 个失败任务都在 0% 退出，根因是旧 candidate 未包含
  `contracts/raw-event-v1.schema.json`，Python 研究模块导入失败。
- candidate 构建现复制整个 `contracts/` 并将其纳入 artifact 指纹，新增打包回归断言。
- 新 candidate `aa58e9256d36-d75ba3a4f586` 已验证合同文件存在、Python adapter 可导入，并以同一
  J 策略和 22,396 条数据版本完成离线回测冒烟，输出 1,279 个公开事件。
- 全仓 Node `255/255`、TypeScript 通过；既有 4273 后端未擅自重启。
