# 前端信息架构与紧凑表单验证

时间：2026-07-22 16:09 Asia/Singapore

补充：同轮完成数据集 manifest/DTO V2 语义、V1 兼容读取，以及数据集、策略版本、终态回测的
确认式安全删除。4174 staging-sim 已登记 2 个仓库外、哈希验证的只读历史版本。最终验证为全仓
Node `254/254`、前端 Vitest `23/23`、TypeScript 与 Vite production build 通过。

回测失败复核：旧 candidate 漏打包运行时 contracts，导致 Python adapter 导入即失败。修复后新
candidate 已完成同一 J 策略/数据集离线回测冒烟（1,279 个公开事件），全仓 Node `255/255`。

## 范围

- 数据集、策略、回测三个连续工作流页面。
- 后端策略展示元数据、前端严格 DTO 解析、响应式页面层级和表单栅格。
- 不包含公网采集、Paper campaign、shadow、live 或 Tauri 恢复。

## 实现事实

- 默认视图统一为当前选择和状态优先，技术 ID、哈希及原始契约降到可展开详情。
- 12 列响应式表单按字段内容分配 2/3/4/6/8/9/12 列宽，移动端收敛为单列。
- 数据集选择显示人类可读名称、事件数和短版本指纹；完整身份仍由后端 ID 与哈希固定。
- 策略注册表拥有用途、研究状态和参数名称、单位、说明；旧定义缺少增强字段时仍安全兼容。
- 策略版本生成时间来自已有 `createdAtUtc`；数据集缺少可信生成时间时显示“未记录”。

## 验证

- `npx tsc -p frontend/tsconfig.app.json --noEmit`：通过。
- `npm run typecheck`：通过。
- `npm run frontend:test`：`23/23` 通过。
- `npm run frontend:build`：通过。
- `npm test`：`254/254` 通过。
- Playwright 桌面/移动：临时独立端口 4374，`4/4` 通过。
- `git diff --check`：通过。
- 1440×1000 人工截图检查：策略、数据集、回测三页无明显溢出，主次层级和字段宽度符合预期。

## 边界

- 默认 Playwright 端口 4174 被既有本地进程占用；未终止或复用该进程。
- 没有把文件 mtime、扫描时间或数据覆盖时间冒充数据集生成时间。
- `LIVE_TRADING_ENABLED=false` 未改变。

## 2026-07-22 回放与竞技场补充验收

- 市场回放事件流调整为宽主栏，100 条分页事件在有界区域内独立横向、纵向滚动；表头吸顶，当前
  事件行高亮。当前后端事件检查器收窄并限制高度，长 ID 和字段值可换行，不再撑开页面。
- 策略竞技场比较按钮显示选择数与请求进行态；后端拒绝不可比较的运行时，在运行选择区显示明确
  错误，不再表现为点击无响应。比较结果表也增加横向滚动保护。
- `npm run frontend:test`：`24/24` 通过。
- `npm run typecheck`、`npm run frontend:build`、`git diff --check`：通过。
- `npm test`：受限沙箱中仅 loopback Web 文件因 `listen EPERM` 失败，其余 `60/60` 通过；允许
  loopback 的沙箱外单独执行 `node --test dist/backend/tests/workbench-web-server.test.js` 为 `7/7`。
- 未启动公网采集、常驻进程或真实交易；`LIVE_TRADING_ENABLED=false` 未改变。
- 后续真实运行复核发现零成交回测合法返回 `winRate=null`；后端结果类型、前端结果/比较解析器和
  两处展示已统一支持不可用胜率，并增加 DTO 合同回归测试。前端 `24/24`、TypeScript、生产构建、
  编译后合同测试和 `git diff --check` 通过。
- 进一步定位到策略工作室曾把参数下限误当初始值，形成几乎必然全程观望的版本。参数合同现提供
  后端拥有的有效默认值，并增加跨参数语义校验；界面首次加载、切换策略和保存后都会生成下一个
  修订版本号，加载旧版本只复制参数，不会把旧版本号作为下一次保存目标。
- 回测页默认选择验证集。结果新增数据分组、时间跨度、执行场景、样本指纹和样本数；竞技场将这些
  字段纳入硬性比较门槛，旧结果缺少范围证据时提示重新运行。J/K 可显式选择最终测试集，L V2 仍只
  允许验证集，防止不同样本集合被并列解释。
- 同一真实历史验证数据组 1,440 个样本的离线诊断产生 J 159、K 189、L V2 46 次成交；这仅证明
  决策、订单、成交和结算链可达，不构成盈利证据。最终验证为前端 `24/24`、策略 `25/25`、全仓
  类型检查、生产构建及格式检查通过；受限环境外的本机服务 15 个子项全部通过。

## 2026-07-22 动态目标仓位与策略版本可解释性补充

- K/J Paper engine 不再把策略计算出的数量直接当订单数量：策略先形成动态目标总仓位，统一
  `target-position-review-v1` 扣除已持仓和在途订单，再按可见深度、现金、单笔、单市场和总敞口审查。
  审查结果明确为 `APPROVED`、`REDUCED` 或 `REJECTED`，并将净优势、目标/当前/在途仓位、预估
  价格/费用、最终数量及原因写入事件。
- 当前 K/J 输入仅有最佳卖一与一档可见深度；页面明确说明价格不是多档 VWAP，未伪造完整深度或
  实盘成交能力。canonical Paper session 继续是订单、成交、仓位和 PnL 的唯一权威，shadow event
  仅用于策略解释。
- 策略工作室新增 L V1 的可见但锁定审计条目，保留 L V2 的研究候选条目；新展示名称采用业务语义，
  内部 ID 仅在技术详情中保留。已保存版本会加载完整参数并与上一版本逐项比较。
- 验证：`npm run typecheck`、`npm run frontend:test -- --run`（`24/24`）、`npm run build`、
  `npm run frontend:build`、编译后的策略管理/KJ engine/前端命令合同测试、`git diff --check` 均通过。
  未启动公网采集、常驻进程或真实交易；`LIVE_TRADING_ENABLED=false` 未改变。

## 2026-07-22 回测谱系命名与历史决策因子补充

- 回测请求现在有后端验证、持久化的运行名称和说明；未填写时由后端以已注册策略展示名、数据集和
  验证/最终测试分组生成。名称进入任务、结果与竞技场比较 DTO，不参与比较门槛。
- 回测实验室和竞技场将该名称放在首位，运行 ID 与请求 ID 仅在可展开技术详情中显示。历史记录没有
  名称时显示“历史运行（未记录名称）”，不从 ID 推断含义。
- 离线历史回测的决策公开 DTO 增补已记录的决策/执行价、费用、目标/当前/批准数量、可见深度与
  风险字段；缺失的旧字段仍为空，界面和下游不得将其当作统一风险审查已经发生。
- 验证：`npm run typecheck`、`npm run frontend:test -- --run`、`npm run build`、
  `npm run frontend:build`、编译后的 backtest/strategy/KJ 单测和 `git diff --check` 通过。
  未启动公网采集、常驻进程或真实交易；`LIVE_TRADING_ENABLED=false` 未改变。

## 2026-07-22 决策与回放的运行名称层级补充

- 决策记录、市场回放的已完成运行选择器和当前上下文优先显示后端运行名称；运行 ID 仅在可展开
  技术详情中出现，旧运行继续明确标识为“未记录名称”。
- 决策账本默认显示格式化 UTC 时间、事件类型及原因/状态；事件检查器将费用、价格、目标仓位和
  风险审查等已允许字段显示为中文业务标签，原始事件 ID 保留在技术详情。
- 验证：`npm run frontend:test -- --run` `24/24`、`npm run typecheck`、`npm run frontend:build` 和
  `git diff --check` 通过。未启动公网采集、常驻进程或真实交易。

## 2026-07-22 历史回测风险语义纠偏

- 历史回测的公开决策 DTO 现将 `intendedQuantity` / `intendedStake` 作为策略意向单独展示；只有源
  事件实际带有目标仓位或风险审查字段时才公布相应字段。旧回测没有跨市场持仓或统一审查证据时保持
  缺失，不以现有单次 sizing 推断。
- 因此，页面可同时解释现行 Paper engine 的 `target-position-review-v1` 和历史回测的意向数量，而不
  混淆两者的证据强度。
- 验证：适配器映射烟测、`npm run build`、编译后的 query/backtest Node 测试、Python 策略单测和
  `git diff --check` 通过。

## 2026-07-22 公共事件展示语义

- 已在既有公共 UI 模块集中事件 UTC 时间格式、字段中文标签和主因摘要。决策记录从同一来源展示价格、
  成本、意向仓位、目标仓位与风险审查字段，不再维护页面私有映射。
- 验证：`npm run frontend:test -- --run` `24/24`、`npm run typecheck`、`npm run frontend:build` 和
  `git diff --check` 通过。

## 2026-07-22 研究参数告警而非任意拒绝

- 删除策略版本保存入口中任意的 J/K `0.01` 优势间隔和 L `maxSignalEdge=0.05` 最低硬门。
  后端现在返回红色“会全程观望”或橙色“区间过窄/可能大量拒绝”的结构化研究告警；前端只显示，不会
  阻止保存研究版本。
- 字段类型、有限数值，以及实际 Paper 下单的统一风险审查继续保持硬边界。
- 验证：`npm run typecheck`、前端 Vitest、后端策略/前端合同测试、Python 策略测试与
  `git diff --check` 通过。

## 2026-07-22 回放与健康页的技术详情降级

- 市场回放的事件流和当前事件检查器以格式化 UTC 时间、类型、原因/状态及中文字段名为默认阅读路径；
  冻结事件 ID 只在可展开的技术详情中显示。
- 已验证本地数据的健康页将实时快照与异常时间标为 UTC，并把异常 ID 收入技术详情；数据仍只来自固定
  后端探针，未用演示运行时状态替代。
- 验证：完整 Node `258/258`（工作台本机回环子集 `10/10`）、Python 策略 `10/10`、
  `npm run typecheck`、前端 Vitest `24/24`、`npm run frontend:build` 与 `git diff --check` 通过。
  Playwright 配置可列出桌面/移动共 4 项，实际运行停在 “Running 4 tests”；仅保留桌面总览和移动健康
  页截图、没有最终通过/失败汇总，故不把它记为 E2E 通过。

## 2026-07-22 回测与竞技场结果语义名称

- 已验证回测结果和竞技场比较均通过固定后端策略/数据集目录展示名称；运行、策略、数据集和事件的
  技术标识只在可展开详情中保留。没有目录记录的旧结果明确显示“未记录名称”，不从内部 ID 推测。
- 验证：`npm run typecheck`、前端 Vitest `24/24`、`npm run frontend:build` 和 `git diff --check` 通过。
