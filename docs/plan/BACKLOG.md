# 当前 Backlog

本文件只登记仍未完成的工作，不重复当前事实或历史证据。优先级和授权边界以 [CURRENT.md](CURRENT.md) 为准。

1. 在新的明确联网批准下，用冻结 K/J 配置积累预注册、可 replay 的多市场 public-paper evidence，并以 campaign cohort 完整汇总。
2. 保持公共 CLOB continuity 为 `UNVERIFIED`，除非获得可证明的 sequence/cursor 证据。
3. 为 L 后继研究先补齐连续 CLOB quote velocity 与可验证的 Chainlink boundary 输入；再一次性预注册候选网格并仅做一次 TRAIN 选择和一次 Validation。
4. 补齐尚未证明的历史 fidelity、逐时点 fee 与结算边界问题；禁止把这些缺口用参数调整或描述性 PnL 掩盖。
5. shadow/live 不在当前 backlog 的可执行范围内；需要独立证据门和用户明确批准。

## 前端设计稿功能接线

以下模块的 HTML/React 静态结构是应持续保留的代码资产。真实数据或命令未接入时，以醒目的
`DEMO DATA`、整体锁定和不可持久化方式展示，不得删除模块，也不得把演示数值当作真实结果：

1. 将生产模式的总览、实时、决策、策略、回测、回放、竞技场和健康页逐页收敛到 Fusion V3
   的完整信息结构；建立同尺寸桌面/移动视觉回归，防止真实接线再次删减布局。
2. 建立统一的 `REAL / DEMO / UNAVAILABLE / ERROR` 模块级状态；当前已有页面级真实/演示切换，
   仍需让同一页面中的每个模块独立混合真实数据和锁定演示数据。
3. 决策记录：接入市场/方向/优势/可执行/成交/异常筛选、CSV 导出和复盘集创建。
4. 策略工作室：保留原代码编辑器外观并明确只读/禁用；若未来允许新增策略，仍必须进入受审查
   注册表，不开放网页任意代码上传或执行。补参数变更日志、扫描和实验台账。
5. 回测实验室：为真实结果补齐毛 PnL、成交笔数、平均每笔、Log Loss、可估/未验证市场、概率
   校准、滚动 Brier、分组表现和可检查的决策账本。
6. 市场回放：从冻结事件重建概率/价格图、双边盘口和市场状态，增加跳到决策、跳到异常；所有
   重建字段必须有事件 provenance，缺字段时显示不可用。
7. 策略竞技场：接入真实累计 PnL 曲线、校准对比、排名卡、保存对比、导出与复盘摘要；继续由
   后端强制相同数据集、费用、延迟、资金和仓位口径。
8. 系统健康：接入服务与信号源状态、数据质量计数器、延迟曲线、诊断下载和异常确认；不能让
   前端开关绕过后端生命周期或联网批准。
9. 恢复并扩充使用说明、版本记录和每个 `DEMO/UNAVAILABLE` 模块的数据来源说明。
10. 数据集增加 Parquet 的有界、只读归一化支持，并补 schema、大小、路径和恶意输入负例。
